<div class="row-fluid">
    <div id="site-collect" class="span12">
        <h3>Collect Subscriptions</h3>

        <div class="alert">
            {{ 'collect_cron_info'|trans }}
        </div>

        <div class="btn-toolbar list-toolbar">

            <div class="btn-group">
                <button class="btn collect-select-all"><i class="icon-check"></i></button>
                <button class="btn collect-select-all"><i class="icon-check-empty"></i></button>
            </div>

            <div class="btn-group">
                <button class="btn dropdown-toggle" data-toggle="dropdown">
                    <i class="icon-cog"></i>
                    <span class="caret"></span>
                </button>
                <ul class="dropdown-menu">
                    <li>
                        <a href="#">A1</a>
                    </li>
                    <li>
                        <a href="#">B2</a>
                    </li>
                    <li>
                        <a href="#">C3</a>
                    </li>
                </ul>
            </div>

        </div>

        <ul class="inline nav nav-pills">
            {% for sub in subscriptions %}
                <li data-sub-id="{{ sub.id }}">
                    <a href="">
                        {#<input type="checkbox"/>#}
                        {% if sub.icon %}
                            <img src="/static/img/subscription-icons/gen/{{ sub.icon }}" alt=""/>
                        {% else %}
                            <img src="/static/img/subscription-icons/default.png" alt=""/>
                        {% endif %}

                        {% if true or sub.error %}
                            {{ sub.name }}
                        {% else %}
                            <span class="alert-danger">{{ sub.name }}</span>
                        {% endif %}
                    </a>
                </li>
            {% endfor %}
        </ul>
        <button name="collect" class="btn">Collect Subsriptions</button>
        <div class="collect-result"></div>
    </div>
</div>